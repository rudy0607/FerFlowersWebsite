// Square API Configuration
const squareConfig = {
    applicationId: 'YOUR_SQUARE_APPLICATION_ID',
    locationId: 'YOUR_SQUARE_LOCATION_ID',
    environment: 'sandbox' // Change to 'production' when going live
};

// Do not expose your access token in client-side code
